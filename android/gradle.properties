# Project-wide Gradle settings.

# IDE (e.g. Android Studio) users:
# Gradle settings configured through the IDE *will override*
# any settings specified in this file.

# Specifies the JVM arguments used for the daemon process.
# Tweaked memory for fast parallel builds and worker thread stability.
org.gradle.jvmargs=-Xmx4096m -XX:MaxMetaspaceSize=1024m -XX:+UseG1GC

# Enable parallel execution and build caching for speed
org.gradle.parallel=true
org.gradle.caching=true
org.gradle.vfs.watch=false

# AndroidX package structure
android.useAndroidX=true
android.enableJetifier=false

# Enable AAPT2 PNG crunching
android.enablePngCrunchInReleaseBuilds=true

# Architectures
reactNativeArchitectures=armeabi-v7a,arm64-v8a,x86,x86_64

# New Architecture & Hermes Engine
newArchEnabled=false
hermesEnabled=true

# Expo Image Formats
expo.gif.enabled=true
expo.webp.enabled=true
expo.webp.animated=false

# Network Inspector
EX_DEV_CLIENT_NETWORK_INSPECTOR=true

# Legacy Packaging
expo.useLegacyPackaging=false
